package ik.learn.trees;

public class TreeDFSPattern {
//    DFS Pattern
//    if(root == null) return;
//    else  dfs(root);

//    dfs (node) {
     // base case - leaf node
//    if(node.left == null && node.right == null) {
        //do something
//      }
     // Recursive case : internal nodes
//      if (node.left != null) dfs(node.left);
//      if (node.right != null) dfs(node.right);
//    }

    // alternate Template that allows DFS callled on non-null node (unlike Graphs)
//    dfs(node) {
        // Base case : null node
//        if(node == null) return;

        // Recursive case: non-null node
//        dfs(node.left);
//        dfs(node.right);
//    }


}
